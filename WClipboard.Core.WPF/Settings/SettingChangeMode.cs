namespace WClipboard.Core.WPF.Settings
{
    /// <summary>
    /// Should the value be adjusted directly or only when the user saves the settings?
    /// </summary>
    public enum SettingChangeMode
    {
        Direct,
        OnSave
    }
}
